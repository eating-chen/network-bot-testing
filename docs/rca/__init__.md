# `data_rca/__init__.py`

## 角色

把 `data_rca` 標記為 Python package。目前只有 package docstring，import 時不會下載資料、build
dataset 或修改檔案。

## Functions

沒有 function、input 或 output。

```python
import data_rca
# -> import 成功；沒有 stdout、檔案寫入或 network request
```
